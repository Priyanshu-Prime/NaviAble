# ──── FastAPI Core ────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ──── Data Validation ────
pydantic>=2.7.0

# ──── Environment configuration ────
python-dotenv>=1.0.0

# ──── Async HTTP & Testing ────
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
anyio>=4.4.0

# ──── ML Inference (structure only — weights loaded separately) ────
torch>=2.0.0
ultralytics>=8.0.0
transformers>=4.30.0
Pillow>=10.0.0
numpy>=1.24.0

