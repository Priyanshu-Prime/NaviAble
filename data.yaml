# Relative path to the split dataset folder (resolved from this file's location)
path: ./NaviAble_Dataset

train: images/train
val: images/val

# Number of classes
nc: 4

# Class names (The order MUST match your conversion script)
names: ['step', 'stair', 'ramp', 'grab_bar']